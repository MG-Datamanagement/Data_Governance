import type { MetadataRoute } from "next";

/**
 * sitemap.ts — No-op stub.
 * This application is a private enterprise tool with no public pages.
 * Extend this function when public landing / marketing pages are added.
 */
export default function sitemap(): MetadataRoute.Sitemap {
  return [];
}
